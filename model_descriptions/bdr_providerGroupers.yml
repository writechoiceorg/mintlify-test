version: 2

models:
  - name: bdr_providerGroupers
    config:
      tags: [ 'bdr' ]
      frequencyDomain: 'BINDERY'
    data_tests:
      - not_empty:
          meta:
            requirement: ['FDR-1404']
    columns:
      - name: providerPersonId
        data_tests:
          - not_null:
              meta:
                requirement: [ 'FDR-1404' ]
