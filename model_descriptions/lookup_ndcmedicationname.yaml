version: 2

models:
    - name: lookup_ndcmedicationname
      config:
          tags: [ 'lookup' ]
      description: >
          Table provides NDC medication names
      data_tests:
          - not_empty:
                meta:
                    requirement: [ 'ETL-836' ]
