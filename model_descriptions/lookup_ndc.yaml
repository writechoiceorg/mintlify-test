version: 2

models:
  - name: lookup_ndc
    config:
      tags: [ 'lookup' ]
    description: >
      Table provides NDC codes
    data_tests:
      - not_empty:
          meta:
            requirement: [ 'ETL-836' ]
