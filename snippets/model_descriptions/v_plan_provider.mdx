export const schema = {
  "version": 2,
  "models": [
    {
      "name": "v_plan_provider",
      "description": "This table contains a master list of all providers known to the payer, along with demographic and taxonomic information and essential identifiers for reimbursement. Providers are individuals or organizations who provide care directly as reflected through claims or to whom members are assigned for purposes of managed care. Any provider ID mentioned in v_plan_claim_header, v_plan_claim_line, v_plan_claim_rx, v_plan_member_elig, or v_plan_member_assignment must be included in this master list.\n",
      "data_tests": [
        {
          "not_empty": {
            "meta": {
              "requirement": [
                "FDR-1849"
              ]
            }
          }
        }
      ],
      "columns": [
        {
          "name": "provider_id",
          "description": "QDW plan provider ID; primary key for this table.  This is generated by Arcadia.  Note that provider IDs used in plan tables are different from the provider IDs used in clinical tables.  In addition, provider IDs are different from billing provider IDs.  But billing provider IDs are found only in the plan_claim_header table; every other provider ID in the plan tables is linked to this column in this table.  (Providers can be crosswalked between clinical and plan sources through the ProviderPersonSource view, which links providers based on their NPI.  If a provider does not have an NPI from a source, one can be added to the ProviderXref table.)\n"
        },
        {
          "name": "orig_provider_id",
          "description": "ID of the plan provider row in the Staging database, used to tie back to the plan data source.  This is created by the connector query or direct feed that loads the row from a given source, and is derived from plan data source column(s) to uniquely identify a row. For a given source, one original ID corresponds to one and only one QDW ID for this entity.  The same original ID value could be found on more than one row if the rows are derived from different sources.\n"
        },
        {
          "name": "business_phone",
          "description": "Provider business phone number.  \n"
        },
        {
          "name": "fax",
          "description": "Provider fax number.  Is typically null.\n"
        },
        {
          "name": "first_name",
          "description": "Provider first name.  When the provider represents an organization such as 'Fremont Endoscopy Center' rather than an individual, this column can be null.  Occasionally the provider's middle initial may be found in this column as well, when the plan data source sends provider names that way.\n"
        },
        {
          "name": "middle_initial",
          "description": "Provider middle initial.\n"
        },
        {
          "name": "last_name",
          "description": "Provider last name.  When the provider represents an organization such as 'Fremont Endoscopy Center' rather than an individual, the full name of the organization should be in this column.\n"
        },
        {
          "name": "sex_unscrubbed",
          "description": "Provider gender, as specified in the plan data source.  This value is scrubbed into the sex column.\n"
        },
        {
          "name": "sex",
          "description": "Provider gender. Valid values are 'M' (Male), 'F' (Female), 'O' (Other; used for transgender or other distinctions) and 'U' (Unknown).  This value is derived from sex_unscrubbed based on mapping in the lookup.plan_sex table in Staging.  \n"
        },
        {
          "name": "dob",
          "description": "Provider date of birth.\n"
        },
        {
          "name": "npi",
          "description": "National Provider Identifier (NPI) code of the plan provider.  NPI codes are maintained by CMS and may represent either an individual provider or a healthcare organization.  Valid NPI codes are 10 digits long with no leading zeroes.  NPI codes do not uniquely identify a given plan provider row.  The same NPI code may be present on multiple plan provider rows, for example when the plan data source uses the organization's NPI code for providers working in that organization who do not have individual NPI codes.  A given plan provider could also be active in multiple plan data sources, in which case there would be a multiple provider_ids (one per plan data source) for the same NPI code, since a QDW plan provider_id is used only on data from a single plan data source.  The NPI code is used to match providers specified in various plan and clinical tables, and the match results are represented in the ProviderPerson table.\n"
        },
        {
          "name": "dea",
          "description": "Drug Enforcement Agency number of the provider.  \n"
        },
        {
          "name": "primary_specialty_unscrubbed",
          "description": "Primary specialty for the provider, as specified in the plan data source.  This value is scrubbed into the primary_specialty column.\n"
        },
        {
          "name": "primary_specialty",
          "description": "Provider's specialty, describing the kind of medicine physicians, non-physician practitioners or other healthcare providers/ suppliers practice.  For provider specialties in both plan_provider and Provider_Master, Arcadia uses a normalized list of provider specialties adapted from a CMS list.  This value is derived from the value in primary_specialty_unscrubbed, using mappings found in Staging table lookup.plan_primary_specialty. \n"
        },
        {
          "name": "license_no",
          "description": "Provider license number.  \n"
        },
        {
          "name": "medicaid_id",
          "description": "Provider Medicaid identifier.  \n"
        },
        {
          "name": "provider_type_unscrubbed",
          "description": "The type of provider, as specified in the plan data source.  This value is scrubbed into the provider_type column.\n"
        },
        {
          "name": "provider_type",
          "description": "Code indicating the type of provider.  The only value currently used in this column is 'PCP', and only on rows where the provider acts as a primary care provider.  This value is derived from provider_type_unscrubbed based on mapping in the lookup.plan_provider_type table in Staging.\n"
        },
        {
          "name": "tax_id",
          "description": "IRS Tax Identification Number, also known as TIN, of the provider.  Valid tax IDs are generally represented as 9 numeric digits.  A tax ID for a business entity is also known as Employer Identification Number (EIN).  \n"
        },
        {
          "name": "taxonomy,",
          "description": "Provider taxonomy.  Healthcare Provider Taxonomy Codes categorize the type, classification, and/or specialization of health care providers.  Valid taxonomy codes are represented as 10 alphanumeric characters, beginning with three numeric digits and often ending in 'X'.  This value is derived from the taxonomy_unscrubbed column based on mapping in the lookup.plan_taxonomy table in Staging. \n"
        },
        {
          "name": "taxonomy_unscrubbed",
          "description": "Provider taxonomy, as specified by the plan data source.  This value is scrubbed into the taxonomy column.  \n"
        },
        {
          "name": "source_id",
          "description": "QDW source ID of the plan data source from which this record was obtained.\n"
        },
        {
          "name": "orig_source_id",
          "description": "QDW source ID in character format of the plan data source from which this record was obtained.\n"
        },
        {
          "name": "orig_provider_id",
          "description": "ID of the plan provider row in the Staging database, used to tie back to the plan data source.  This is created by the connector query or direct feed that loads the row from a given source, and is derived from plan data source column(s) to uniquely identify a row. For a given source, one original ID corresponds to one and only one QDW ID for this entity.  The same original ID value could be found on more than one row if the rows are derived from different sources.\n"
        },
        {
          "name": "insert_timestamp",
          "description": "Date and time when the row was first inserted into the QDW.\n"
        },
        {
          "name": "update_timestamp",
          "description": "Date and time when the row was last updated in the QDW.\n"
        },
        {
          "name": "create_timestamp",
          "description": "Date and time when the row was first inserted into the plan data source.\n"
        },
        {
          "name": "modify_timestamp",
          "description": "Date and time when the row was last updated in the plan data source.\n"
        },
        {
          "name": "created_by",
          "description": "User who first inserted the row into the plan data source, as specified by a user ID known to the plan data source.\n"
        },
        {
          "name": "modified_by",
          "description": "User who most recently updated the row in the plan data source, as specified by a user ID known to the plan data source.\n"
        },
        {
          "name": "delete_ind",
          "description": "Indicates if the record is marked as deleted, either because it was deleted in the plan data source, or because the connector query changed in a way that makes the record no longer valid.  Values are Y (deleted) and N (not deleted).  If the value is 'Y' then the row should be excluded from all queries.\n"
        },
        {
          "name": "loaded_from_file",
          "description": "Name of the file from which this particular record was first loaded, usually a CSV file. The following pieces of information may be embedded in this file name: date and time of the extract, client and source IDs, and connector query version.\n"
        },
        {
          "name": "rc_source_id",
          "description": "The Arcadia source ID of the plan data source from which this record was obtained.\n"
        }
      ]
    }
  ]
};
