export const schema = {
  "type": "object",
  "title": "Asynchronous Field Parser",
  "description": "Send the the contents of a field to the async-parsing service, to be processed using the given index profile.",
  "required": [
    "profileId"
  ],
  "properties": {
    "skip": {
      "type": "boolean",
      "title": "Skip This Stage",
      "description": "Set to true to skip this stage.",
      "default": false,
      "hints": [
        "advanced"
      ]
    },
    "label": {
      "type": "string",
      "title": "Label",
      "description": "A unique label for this stage.",
      "hints": [
        "advanced"
      ],
      "maxLength": 255
    },
    "condition": {
      "type": "string",
      "title": "Condition",
      "description": "Define a conditional script that must result in true or false. This can be used to determine if the stage should process or not.",
      "hints": [
        "code",
        "code/javascript",
        "advanced"
      ]
    },
    "sourceField": {
      "type": "string",
      "title": "Source Field",
      "description": "The document field where the content should be read from.",
      "default": "_raw_content_"
    },
    "sourceEncoding": {
      "type": "string",
      "title": "Source Field Encoding",
      "description": "The content transport encoding of the data stored in the source field (per RFC1341).",
      "enum": [
        "binary",
        "base64"
      ],
      "default": "binary"
    },
    "profileId": {
      "type": "string",
      "title": "Profile ID",
      "description": "The ID of the index-profile to use.",
      "reference": "parser"
    },
    "sourceContentType": {
      "type": "string",
      "title": "Default source Content Type",
      "description": "The default Content-Type of the data stored in the source field."
    },
    "inheritFields": {
      "type": "boolean",
      "title": "Inherit Fields",
      "description": "Should generated documents include the original document's fields.",
      "default": true
    },
    "continueProcessing": {
      "type": "boolean",
      "title": "Continue processing",
      "description": "When enabled, the current document will be passed to the next stage in the pipeline. Otherwise, discard the document.",
      "default": false
    }
  },
  "category": "Document Transformation",
  "categoryPriority": 9,
  "unsafe": false
}
